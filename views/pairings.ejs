<div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
  <div class="mb-8">
    <h1 class="text-3xl font-bold text-gray-900">Genetic Pairing Simulations</h1>
    <p class="mt-2 text-gray-600">Discover compatible pairings for optimal genetic diversity</p>
  </div>

  <div class="mb-6">
    <form method="POST" action="/pairings/generate">
      <button
        type="submit"
        class="px-6 py-3 bg-blue-600 text-white rounded-md hover:bg-blue-700 transition font-medium shadow-md"
      >
        Generate Pairing Recommendations
      </button>
    </form>
  </div>

  <% if (pairings && pairings.length > 0) { %>
  <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
    <% pairings.forEach((pairing, index) => { %>
    <div class="bg-white shadow-md rounded-lg border border-gray-200 p-6 hover:shadow-lg transition">
      <div class="flex items-start justify-between mb-4">
        <div>
          <h3 class="text-lg font-semibold text-gray-900">
            Match #<%= index + 1 %>
          </h3>
          <p class="text-sm text-gray-500 mt-1">
            Partner ID: <%= pairing.partner.id.substring(0, 8) %>...
          </p>
        </div>
        <span class="px-4 py-2 text-lg font-bold rounded-full <%= pairing.match_score >= 70 ? 'bg-green-100 text-green-800' : pairing.match_score >= 40 ? 'bg-yellow-100 text-yellow-800' : 'bg-gray-100 text-gray-800' %>">
          <%= pairing.match_score %>%
        </span>
      </div>

      <div class="space-y-3">
        <div class="flex items-center">
          <svg class="w-5 h-5 text-gray-400 mr-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z"></path>
          </svg>
          <div>
            <p class="text-sm font-medium text-gray-900">
              <%= pairing.partner.name || 'Anonymous User' %>
            </p>
            <p class="text-xs text-gray-500"><%= pairing.partner.email %></p>
          </div>
        </div>

        <% if (pairing.partner.age) { %>
        <div class="flex items-center">
          <svg class="w-5 h-5 text-gray-400 mr-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z"></path>
          </svg>
          <p class="text-sm text-gray-600">Age: <%= pairing.partner.age %></p>
        </div>
        <% } %>

        <% if (pairing.partner.region) { %>
        <div class="flex items-center">
          <svg class="w-5 h-5 text-gray-400 mr-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17.657 16.657L13.414 20.9a1.998 1.998 0 01-2.827 0l-4.244-4.243a8 8 0 1111.314 0z"></path>
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 11a3 3 0 11-6 0 3 3 0 016 0z"></path>
          </svg>
          <p class="text-sm text-gray-600">Region: <%= pairing.partner.region %></p>
        </div>
        <% } %>

        <div class="pt-3 border-t border-gray-200">
          <div class="flex items-center justify-between">
            <span class="text-sm font-medium text-gray-700">Genetic Diversity</span>
            <span class="px-3 py-1 text-xs font-semibold rounded-full <%= pairing.diversity_level === 'high' ? 'bg-green-100 text-green-800' : pairing.diversity_level === 'medium' ? 'bg-yellow-100 text-yellow-800' : 'bg-gray-100 text-gray-800' %> capitalize">
              <%= pairing.diversity_level %>
            </span>
          </div>
        </div>

        <div class="pt-3">
          <div class="w-full bg-gray-200 rounded-full h-2">
            <div class="<%= pairing.match_score >= 70 ? 'bg-green-600' : pairing.match_score >= 40 ? 'bg-yellow-500' : 'bg-gray-400' %> h-2 rounded-full" style="width: <%= pairing.match_score %>%"></div>
          </div>
          <p class="text-xs text-gray-500 mt-1 text-center">
            Compatibility Score: <%= pairing.match_score %>/100
          </p>
        </div>
      </div>

      <p class="text-xs text-gray-400 mt-4 text-center">
        Generated: <%= new Date(pairing.created_at).toLocaleDateString() %>
      </p>
    </div>
    <% }); %>
  </div>
  <% } else { %>
  <div class="bg-white shadow-md rounded-lg border border-gray-200 p-12 text-center">
    <svg class="mx-auto h-16 w-16 text-gray-400 mb-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 20h5v-2a3 3 0 00-5.356-1.857M17 20H7m10 0v-2c0-.656-.126-1.283-.356-1.857M7 20H2v-2a3 3 0 015.356-1.857M7 20v-2c0-.656.126-1.283.356-1.857m0 0a5.002 5.002 0 019.288 0M15 7a3 3 0 11-6 0 3 3 0 016 0zm6 3a2 2 0 11-4 0 2 2 0 014 0zM7 10a2 2 0 11-4 0 2 2 0 014 0z"></path>
    </svg>
    <h3 class="text-xl font-semibold text-gray-900 mb-2">No Pairings Generated Yet</h3>
    <p class="text-gray-600 mb-6">Generate pairing recommendations by adding family health history and clicking the button above.</p>
    <a href="/history" class="inline-block px-6 py-3 bg-blue-600 text-white rounded-md hover:bg-blue-700 transition font-medium">
      Add Family History
    </a>
  </div>
  <% } %>

  <div class="mt-8 bg-purple-50 border border-purple-200 rounded-lg p-6">
    <h3 class="text-lg font-semibold text-purple-900 mb-3">How Pairing Works</h3>
    <div class="space-y-2 text-sm text-purple-800">
      <p><strong>Match Score:</strong> Higher scores indicate greater genetic diversity between partners, which may reduce the risk of passing on hereditary conditions.</p>
      <p><strong>Diversity Level:</strong> Indicates the overall genetic variation between two individuals based on their family health histories.</p>
      <p><strong>Note:</strong> These simulations are for educational purposes only and should not be used for actual relationship decisions.</p>
    </div>
  </div>
</div>
